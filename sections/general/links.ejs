::: {.link-cards}

<% 
const columns = [[], [], []]; // Arrays to hold links for each column
items.forEach(item => {
  item.links.forEach((link, index) => {
    columns[index % 3].push(link); // Distribute links into columns
  });
});
%>

<% columns.forEach((column, colIndex) => { %>
::: {.card}
::: {.card-body}
<% column.forEach(link => { %>
[<%= link.text %>](<%= link.href %>)
<% }); %>
:::
:::
<% }); %>

:::